export interface ComisionProps {
  id: number;
  nombre: string;
  profe: string;
  cupo: number;
  inscriptos: number;
  turno: string;
  nivel: string;
}

export const comisiones: ComisionProps[] = [];
